Current runs for a long time and then gets Killed.

Not entirely sure why. May be too many environments.

Trying it with only 8 instead of 2048 as given by the tutorial
Doing it with only 8 environments seems better.
Must be some timeout thing for long processes.